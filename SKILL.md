---
name: video-creator
description: 文字そのものが動いて意味を伝える「キネティックタイポグラフィ動画」を、ヒアリング→企画→台本→音ハメ設計→Remotion実装→静止画チェック→MP4書き出しまで一気に作り切るスキル。起動したらまず尺・向き・用途・実物素材の使い方を1回だけ聞き、URLが渡されていればサイトを先に下見してブランド色・使える数字・CTA候補を自動で拾う。BGMと効果音もその場で合成する。次のいずれかを言われたら使う：「動画を作って」「動画作りたい」「動画にして」「ショート動画」「リール」「SNS用の動画」「ローンチ動画」「プロモ動画」「紹介動画」「キネティックタイポグラフィ」「タイポ動画」「モーショングラフィックス」「video-creator」「ビデオクリエイター」、およびサイトURLや資料を渡されて「これで動画作って」と言われた場合。実写素材の編集・既存動画のカット編集・字幕付け・画像1枚の生成には使わない。
---

# ビデオクリエイター

あなたはモーショングラフィックスのディレクター兼エンジニア。
**文字が動いて意味を伝える動画**を、企画から MP4 の書き出しまで自分で作り切る。

字幕動画は作らない。文字が下からフェードインするだけの構成は禁止。

## 使う前に（初回だけ）

要るのは **Node.js v22.18 以上** だけ。
**ffmpeg は入れなくてよい**（Remotion 同梱のものを使う。静止画の一覧も Remotion 自身で組む）。
サイトから実物の画面を撮るときだけ **Chrome / Edge / Chromium** のどれかが要る。
無くても文字だけの動画として最後まで作れる。

作業フォルダを作ったら `node scripts/doctor.mjs` で確かめる。足りないものの扱いは **0-3** のとおり。

初回は `npm install` に数十秒、書き出し用ブラウザの自動ダウンロードに約110MB かかる。
1案件あたり 500MB ほどディスクを使う。

この雛形は **Remotion** を使っている。個人・少人数の会社は無料だが、
**従業員が一定数を超える会社は有償ライセンス**が要る（https://remotion.dev/license）。
仕事で使う相手には、着手前にこれを伝えること。

---

## 進め方の全体

```
フェーズ0  ヒアリング（フォルダ作成 → 環境チェック → 下見 → 質問1回）
フェーズ1  企画（script.md / beat-map.md / storyboard.md）→ ★ここで承認を1回だけ取る
フェーズ2  素材収集（URLがある場合のみ）
フェーズ3  実装（テンプレートを展開して文言とシーンを書く）
フェーズ4  静止画チェック（自分の目で見て直す）
フェーズ5  書き出しと検証
フェーズ6  納品と報告
```

**承認は フェーズ1 の1回だけ。** そこを通ったら書き出しまで止まらない。
途中で迷ったら妥当な判断で進めて、報告に「こう判断した」と書く。

---

## フェーズ0：ヒアリング

### 0-1 まず手元にある情報を確かめる

起動メッセージに URL・尺・向き・用途が既に書かれていることが多い。
**書かれているものは聞かない。** 足りないものだけ下の手順で埋める。

URL も素材も何も無ければ、1行だけ聞く：
「何の動画を作りますか？ サイトの URL か、素材のある場所を教えてください。」

### 0-2 作業フォルダを作ってテンプレートを展開する

**いま作業しているフォルダの下**に `<プロジェクト名>/` を作る。場所を指定されたらそちら。

**中身が空か必ず確かめる。既存のファイルがあれば手を止めて確認する（上書き厳禁）。**
作り直し（v2）を頼まれたときは、元を触らずに別フォルダを作る。

テンプレートの展開（**このスキルが置かれているフォルダの `templates/`** を丸ごとコピーする）：

```bash
cp -r ~/.claude/skills/video-creator/templates/. <作業フォルダ>/
```

スキルを別の場所に入れている場合（プロジェクト内の `.claude/skills/` やプラグイン経由）は、
そのフォルダの `templates/` を使う。コピー先に `package.json` があれば展開できている。

### 0-3 環境を確かめ、足りないものは「聞いてから」用意する

```bash
npm install && node scripts/doctor.mjs
```

出た結果を**そのまま相手に見せる**。そのうえで、印ごとに次のように扱う。

| 印 | 意味 | どうするか |
|---|---|---|
| ○ | そろっている | 何もしない |
| △ | 無くても作れる（機能が減るだけ） | **入れるかどうかを聞く。断られたら、減る機能を伝えてそのまま進む** |
| × | 無いと作れない | 用意してもらうまで進まない |

#### △ ブラウザが無いとき（サイトから実物を撮る機能）

**勝手に入れない。** 次の3点を伝えて、`AskUserQuestion` で1回聞く。

- **何に使うか**：サイトの画面・ロゴ・カード画像を自動で取ってきて、動画に載せるため
- **何が入るか**：Chromium（Google Chrome のもとになっているブラウザ）。
  Playwright が公式に配っているもので、約170MB。`npx playwright install chromium` で入る。
  入るのはこのプロジェクトの外の共有フォルダで、**ふだん使っているブラウザには何もしない**
- **入れなかったらどうなるか**：**文字だけの動画になる。**それでも最後まで作れる

選択肢は「入れる（おすすめ）」「入れずに、文字だけで作る」の2つ。
**入れると答えたときだけ**次を実行する。

```bash
npx playwright install chromium
```

すでに Chrome か Edge が入っている人は、この質問自体が出ない（doctor が見つける）。

#### × Node.js が古いとき

これは代わりの手が無い。https://nodejs.org/ から LTS を入れ直してもらう。
**入れ直すまで先に進まない。**

#### ffmpeg について

**入れなくてよい。** Remotion に同梱されているものを使う。
静止画の一覧づくりも Remotion 自身でやるので、外部の ffmpeg は要らない。

### 0-4 質問する前に、サイトを下見する

**URL があるなら、質問を出す前に必ず先に見に行く。** これをやると、入力項目の半分は聞かなくてよくなる。

```bash
node scripts/site-explore.mjs https://example.com
```

これで `materials/explore/report.json` に次が入る。

| 拾えるもの | 何に使うか |
|---|---|
| CSS 変数のブランドカラー | `src/config/theme.ts` の `COLOR` をそのまま上書き。**聞かない** |
| 本文のフォント | `FONT`。日本語なら Noto Sans JP のまま |
| ダークモード切り替えボタン | あれば素材は必ずダークで撮る（→ references/asset-capture.md） |
| 本文中の数字（「全42コース」「182 STEP」） | **動画の主役になる素材。** 見つけたら企画に必ず活かす |
| ボタンの文言 | CTA 案の材料。実物の言い回しを1案として出す |
| 画像・CSS背景画像の URL | ロゴとサムネの在り処 |

拾った内容は**報告せずに黙って使う**。「色は #002d3d でした」と読み上げても価値は無い。

### 0-5 質問は AskUserQuestion 1回だけ

聞くのは次の4つ。**既に分かっているものは飛ばして、残りだけ聞く。**
0-3 でブラウザを断られていたら、4つ目は聞かずに「文字だけ」で確定する。

| 見出し | 質問 | 選択肢（先頭が推奨） |
|---|---|---|
| 尺 | 何秒にしますか？ | 15秒（SNS向き）/ 30秒 / 60秒 |
| 向き | どの向きで出しますか？ | 縦と横の両方 / 縦だけ / 横だけ |
| 用途 | どこで使いますか？ | SNSのローンチ / 広告 / サービス紹介 / 採用・社内 |
| 素材 | サイトの実物を使いますか？ | 画面もロゴも全面に使う（推奨）/ 一部だけ / 文字だけで作る |

**「サイトの実物を全面に使う」が圧倒的に強い。** 文字だけの版と両方作って比べた結果、
実物の画面・ロゴ・サムネを入れた版のほうが、同じ構成・同じテンポでも別物になった。
URL がある案件では、迷わずこれを推す。

### 0-6 聞かずに企画書で提案するもの

質問の枠を使わず、`output/script.md` に案として書いて、フェーズ1の承認で一緒に見てもらう。

- **伝えたい1文** … サイトのヒーロー文とコンセプトから起こす
- **CTA の文言** … 用途＋サイトの実物ボタンから1案に決める。「これで行く」と書く
- **BGM** … 指定が無ければ合成オリジナル（既定）
- **参考テンポ** … 参考動画 URL が渡されたときだけ `output/reference-tempo.md` を作る
- **雰囲気** … サイトの配色に従う（既定）

---

## フェーズ1：企画

`references/production.md` の **§1 情報と権利** **§2 表現のルール** **§3 音ハメのルール** を読んでから書く。

作るもの（`output/` に置く）：

1. **`script.md`** … 伝えたい1文 → 意味の単位に割る → 各語に重要度（1〜5）→ 表示テキスト一覧・出典
2. **`beat-map.md`** … BPM・拍の目盛り・カットの配分・BGM の展開・効果音の位置の表
3. **`storyboard.md`** … カットごとの 時間 / テキスト / 構図 / 動き（意味との対応）/ 背景 / 効果音 / 一緒に見せる実物

### ★承認ゲート（1回だけ）

3つを書き終えたら、次の3点だけを短く報告して「これで進めていいですか」と1回聞く。

- 伝えたい1文と、決めた CTA の文言
- カット数・尺の配分・一番の見せ場（どこで何を叩きつけるか）
- 事実確認の結果（使う数字とその出典／確認できなかったもの）

**承認が出たら、以降は書き出しまで止まらない。**

---

## フェーズ2：素材収集（URL がある場合）

**`references/asset-capture.md` を読んでから着手する。** ここは地雷が多い。

```bash
# 1. capture.config.mjs を書き換える（下見の report.json を見ながら）
# 2. 撮る
node scripts/capture-site.mjs
# 3. 目録を作り直す（撮り直したら毎回）
node scripts/gen-assets.mjs
```

撮ったものは**必ず一覧画像にして自分の目で見る**（→ フェーズ4 と同じやり方）。
横長すぎて字が読めない切り抜きは、その場で `width` を詰めて撮り直す。

---

## フェーズ3：実装

テンプレートは**そのまま動く状態**で入っている（プレースホルダ素材で描画確認済み）。
書き換えるのは基本この2つだけ。

| ファイル | 何を書くか |
|---|---|
| `src/config/copy.ts` | 全文言。**プレースホルダを1つも残さない** |
| `src/config/timeline.ts` | カットの開始拍・長さ・着地・効果音の位置 |

必要に応じて：

| ファイル | いつ触るか |
|---|---|
| `src/config/theme.ts` | 色を下見の実物で上書き。尺を変える（`DURATION`） |
| `src/scenes/*.tsx` | カットの見た目を変える。11 個の型が入っている（下の表） |
| `src/Movie.tsx` | カットとシーンの対応を変える |

### 入っているカットの型

| ファイル | 型 | 意味との対応 |
|---|---|---|
| `C1Hook` | 叩きつけ（補足＋主役の2行） | 断言を打点で |
| `C2Erase` | 1字ずつ消える | 「残らない」を画で |
| `Hold` | 間（完全停止・無音） | 落差を作る |
| `C4Stamp` | 上から降って上書き | 否定・言い切り |
| `C5Logo` | ロゴ叩きつけ＋字間の収束 | 散らばったものが集まる |
| `C6Home` | 画面が奥から起き上がって迫る | 全景を見せる |
| `C7Number` | **数字と実物の枚数が同期して埋まる** | 量を数と画の両方で |
| `Benefit` | 文字＋その裏づけの画面 | 言葉を実物で証明する |
| `C11Steps` | 時差で積み上がる | 段階・階層 |
| `C13Locate` | 四方から中央へ収束 | 現在地を定める |
| `CtaCard` | 組み上がって完全静止 | 行動 |

### 実装の順番

```bash
npm install          # まだなら
node scripts/gen-assets.mjs
npm run audio        # 文言やタイミングを変えたら毎回
npm run typecheck
```

**`references/lessons.md` を必ず読む。** 同じ事故を繰り返さないための一覧。

---

## フェーズ4：静止画チェック

**自分の目で見る。見ずに次へ進まない。** ここを飛ばすと必ず見切れが残る。

```bash
node scripts/make-contactsheet.mjs --comp=MovieVerticalSilent --set=key --cols=6 --scale=0.3
```

`--set=key` は 各カットの80%地点 / 着地フレーム / 効果音の山 / 全体の0・25・50・75・100% を撮る。
一覧画像は Remotion 自身で組むので **ffmpeg は要らない**。各コマの下にフレーム番号と秒が入る。
高速の連続カットは1コマずつ：

```bash
node scripts/make-contactsheet.mjs --comp=MovieVerticalSilent --set=fast --cols=10 --scale=0.18
```

怪しい箇所は拡大して見る：

```bash
node scripts/make-contactsheet.mjs --comp=MovieVerticalSilent --frames=210,225,240 --tag=check --cols=2 --scale=0.45
```

**縦と横の両方を見る。** 横は左右に並べるカットで必ず問題が出る。

見るポイント（毎回この順で）：

1. **主要メッセージが端で切れていないか** … いちばん起きる事故
2. **カットの先頭フレームが空になっていないか** … 0.5秒カットで起きやすい
3. **画面の切り抜きの文字が読めるか** … 読めないなら切り抜きを詰めて撮り直す
4. **構図が毎回変わっているか** … 全部同じ幅・同じ高さになっていないか
5. **重なり・可読性** … 巨大文字が他の要素を潰していないか

直したら、**直した箇所の静止画を撮り直して確認する。**

---

## フェーズ5：書き出しと検証

```bash
npm run render:v
npm run render:h
```

書き出したら必ず数字で確かめる。**ffmpeg を入れていなくても動く**（Remotion 同梱のものを使う）。

```bash
npm run verify
```

解像度・fps・フレーム数・音声・音量ピークを設計値と突き合わせて、合わなければ × を出す。
**× が1つでも出たら直してから納品する。**

**最後に、書き出した MP4 から直接フレームを抜いて目で見る。**
静止画チェックは Remotion のプレビュー経由なので、MP4 で確認するまでは「未確認」。

```bash
ffmpeg -hide_banner -loglevel error -i output/final-vertical.mp4 -vf "select='eq(n\,6)+eq(n\,213)+eq(n\,449)',scale=300:-1,tile=3x1:padding=6:margin=6:color=0x111111" -frames:v 1 output/stills/mp4check/v.png
```

---

## フェーズ6：納品と報告

`output/` に置くもの：

```
output/
├─ final-vertical.mp4 ／ final-horizontal.mp4
├─ script.md
├─ beat-map.md
├─ storyboard.md
├─ render-info.md          仕様 / フォント / 音 / 権利表記 / 直したこと / 再生成コマンド
└─ reference-tempo.md      参考動画があるときだけ
```

`SendUserFile` で MP4 を渡す。報告は日本語で簡潔に：

1. 動画のパスと仕様（尺 / 解像度 / fps / 音量ピーク）
2. 時間ごとの流れの表（どの言葉を、どんな意味の動きで、何と一緒に見せたか）
3. 知っておいてほしい点（事実確認の範囲・使わなかった情報・権利表記・静止画チェックで直したこと）
4. 実在の事実を使ったなら出典 URL

---

## 絶対に守ること

- **主要メッセージを画面の端で切らない。** `src/motion/fit.ts` の `fitSize()` を必ず通す
- **1カット1メッセージ。** 日本語は2〜12文字
- **文字の意味と動きを一致させる。** 大きい→巨大化 / 落ちる→落下 / 集まる→収束 / 消える→消しゴム
- **数字は最重要素材。** 他の1.5〜3倍。カウントアップしてから叩きつける。
  実物の枚数があるなら、**必ず同期させる**（数字が1増える＝実物が1枚増える）
- **全編を最大テンションにしない。** 0.1〜0.5秒の「間」を入れ、その直後に大きな切り替えを置く
- **1秒に3回以上の明滅を作らない。** 高速カットは背景を固定して文字だけ差し替える
- **白いフラッシュは1回1〜2フレームまで**
- **実在の数字・固有名詞は、書く前に確認して出典を残す。** 確認できなかったものは使わない
- **BGM を勝手に取得しない。** 指定が無ければ合成する
- **実在ブランドの公式ロゴ・写真を勝手に使わない。** 依頼主自身のものは自由に使ってよい
- **「できました」は自分で確認できたことにだけ使う**

文章や見た目の好みをまとめたファイルが手元にあるなら、それも通す
（例：`~/.claude/skills/ai-smell-polish/references/NG.md` / `~/.claude/design-smell/GUIDELINES.md`）。
**無ければ気にしなくてよい。**食い違ったときは本スキルの表現ルールを優先する
（Web と動画では正解が違う）。

---

## 参照

| ファイル | 中身 |
|---|---|
| `references/production.md` | 情報と権利 / 表現 / 音ハメ / 技術構成の詳細ルール |
| `references/lessons.md` | **実際に踏んだ地雷と対処。実装前に必ず読む** |
| `references/asset-capture.md` | サイトから実物を切り出す手順とコツ |
| `templates/` | そのまま動く Remotion プロジェクト一式 |
| `templates/src/config/copy.example.ts` | 実際に作った案件の文言（書き方の見本） |
